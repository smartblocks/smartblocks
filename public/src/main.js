requirejs([
    "underscore",
    "backbone",
    "UserModel",
    "UsersCollection",
    "LoadingScreen",
    "bootstrap",
    'less'
], function (_, Backbone, User, UsersCollection, LoadingScreen, bootstrap) {

        bootstrap.init(function () {

        });
});