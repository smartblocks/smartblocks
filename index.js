/**
 * This module is a public object that will give the developer access to several important objects, like
 * models, or other stuff.
 */

var smartblocks = {
        models: require('lib/models/access')
    };

module.exports = smartblocks;