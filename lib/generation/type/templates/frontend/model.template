define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    var Model = Backbone.Model.extend({
        idAttribute: '_id',
        default: {

        },
        urlRoot: "/{{ block_name }}/{{ controller_name }}"
    });
    return Model;
});