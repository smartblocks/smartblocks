define([
    'jquery'
    //new_apps_deps - don't remove that comment
], function ($/*new_deps*/) {
    var main = {
        init: function () {

        }
        //new_apps - don't remove that comment
    };
    return main;
});